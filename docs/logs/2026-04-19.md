# 2026-04-19 (Sun) cx-link

## 19:36 — v2.4.0 MCP bridge 統合

### Done
- `lib/mcp-bridge.js` 新設: Chrome拡張 ⇄ browser-tabs-mcp の WebSocket client
  - 4アクション: `get_active_tab` / `get_all_tabs` / `list_tab_groups` / `get_tabs_in_group`
  - 指数バックオフで再接続、`chrome.alarms` で30秒keepalive
- `background.js` に `importScripts('lib/mcp-bridge.js')` 追加
- `manifest.json` 2.3.3 → 2.4.0、description に「MCPブリッジ」追記
- commit `cef9384`、tag `v2.4.0` push 済

### Why
- 「別拡張を作る」より「既存 cx-link に MCP bridge を足す」が ROI 高。1拡張=1目的の原則より、機能集約で運用コスト削減を優先
- Claude Code から現在のブラウザタブを参照できるようになり、URL triage / 調査系タスクが即時に走る

### Changed
- `lib/mcp-bridge.js` (new)
- `background.js` (modified — importScripts 1行追加)
- `manifest.json` (version + description)

### Next
- browser-tabs-mcp の port 8766 競合問題: Claude Code セッションが複数起動すると同じ port を各セッションが取り合う設計欠陥あり
- 根本治療は launchd broker daemon 分離（WS listener を1つに集約、MCP server は client として接続）→ 後日
- 今は拡張側で最新セッションの server へ繋がる挙動でしのぐ

### Report Impact

#### DX全体会向け
- Chrome 拡張 Link Summarizer v2.4.0 リリース。Claude Code から開いているブラウザタブを参照する MCP bridge を統合
